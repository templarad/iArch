
interface component Observer {
	void update() :

}

interface component Subject {
	void update() :
	void removeObserver() :
	void _notify() :
	void getState() :
	void setState() :
	void addObserver() :

}

Subject = (Subject.setState->Observer.update->Subject._notify->Subject.getState->Subject);
Observer = (Observer.update->Subject.getState->Observer);
Subject = (Subject.setState->Observer.update->Subject.getState->Subject);
Observer = (Observer.update->Subject.getState->Observer);

